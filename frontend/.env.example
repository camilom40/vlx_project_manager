# ============================================================
# Gestor de Proyectos Vitralux / VLX — Frontend (Next.js)
# Copiar a .env.local y completar con valores reales.
# ============================================================

# URL base de la API (backend Express)
# Local: http://localhost:4000 — Railway: URL pública del servicio backend
NEXT_PUBLIC_API_URL=http://localhost:4000
